package-lock.json
pnpm-lock.yaml
yarn.lock

# Cursor session exports — formatting churn should not block CI
.specstory/
.cursor/
.husky/
.vscode/

# Git worktrees — run `yarn fix` inside the worktree, not from root
worktrees/

# TanStack Router plugin output — formatting must match the generator, not Prettier
src/routeTree.gen.ts

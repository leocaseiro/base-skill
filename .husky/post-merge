#!/usr/bin/env bash
# Post-merge hook.
#
# Auto-prune was removed because it silently destroyed uncommitted work in
# worktrees whose branch HEAD happened to be an ancestor of master (e.g. a
# branch created from master where the spec commit hadn't been pushed yet).
# Run `./scripts/prune-merged-worktrees.sh` manually instead.

set -euo pipefail
